# Supabase — Settings → API in your Supabase dashboard
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# OpenRouter — the key already used in the beta
OPENROUTER_API_KEY=

# Whop — generated when you create the webhook endpoint (next step, not yet)
WHOP_WEBHOOK_SECRET=
